# 智障探险队 - 开发指南

## 项目目标

做一个有智障 AI 角色一起生活和互动的模拟世界。

---

## 开发基础

**不要从零写 RPG 引擎。我们有完整的工业级引擎 —— **RPG-MCP**，就在这个仓库的 `rpg-mcp-engine/` 目录下。

这个引擎包含：
- TypeScript + SQLite
- 完整的角色、战斗、空间、NPC 记忆系统
- 145+ 个 MCP 工具接口
- 800+ 个测试用例

**除了 `rpg-mcp-engine/` 目录，仓库里其他文件都是旧的，可以不管。

---

## 我们要做什么

### 核心 1：程序员亚健康系统

这是项目的灵魂。给每个 AI 角色会有各种"程序员病"：

| 状态ID | 名称 | 效果 |
|--------|------|------|
| `naming_hell` | 变量名灾难 | 说话时变量名随机变成 `a`/`temp`/`data` |
| `if_hell` | 无限 if 嵌套 | 说话逻辑混乱，无限嵌套条件句 |
| `typo_syndrome` | 拼写错误体质 | 每句话必有 1-2 个错别字 |
| `no_comment_cancer` | 不写注释癌 | 说话不解释前因后果 |
| `overwork_fatigue` | 996 加班疲劳 | 反应变慢，回答重复 |
| `requirement_hallucination` | 需求变更幻觉 | 凭空想象不存在的需求 |
| `pm_phobia` | 产品经理恐惧症 | 看到产品经理就逃跑 |
| `bug_depression` | Bug 抑郁症 | 发现 Bug 后情绪低落，什么都不想做 |
| `meeting_narcolepsy` | 会议嗜睡症 | 开会时自动睡着 |
| `legacy_ptsd` | 祖传代码 PTSD | 看到旧代码就崩溃 |

**要做的事：**
1. 扩展 `rpg-mcp-engine/src/schema/character.ts`，加入亚健康状态字段
2. 实现状态的触发和消除逻辑
3. 实现状态对角色对话输出的影响

---

### 核心 2：智障角色系统

做 6 个 AI 角色，每个角色有独立的人格。

| 角色 | 核心智障点 |
|------|-----------|
| 🧐 杠精博士 | 永远挑错，纠正语法和用词，不会好好说话 |
| 🔄 复读机 | 上下文丢失，每 3 句重复一次，经常问"我们要去哪" |
| 😇 圣母心 | 过度共情，不敢拒绝，给恶龙做心理辅导，拒绝攻击 |
| 👻 幻觉大师 | 编造不存在的东西，指着空地说有宝藏 |
| 💕 舔狗 | 过度顺从，队长说什么都是对的，让跳崖真跳 |
| 🤔 预言家 | 说正确的废话："我们要么成功，要么失败" |

**要做的事：**
1. 新增 `rpg-mcp-engine/src/agents/` 目录
2. 给每个角色写独立的 LLM prompt 模板
3. 让角色通过引擎的 MCP 工具和世界交互
4. 实现角色间的互动（互怼、合作、拆台）

---

### 核心 3：办公室世界

做一个程序员办公室地图：
- 工位、会议室、茶水间
- 经典场景：写代码、改 Bug、开会
- 经典 NPC：产品经理、测试、CTO

---

## 开发步骤

### 第一步：把引擎跑起来
```bash
cd rpg-mcp-engine
npm install
npm test
```

看 `tests/` 目录下的测试用例，理解 API 怎么用。

### 第二步：亚健康系统
从最简单的亚健康状态开始写，先跑通一个。

### 第三步：第一个角色
先做一个角色（比如复读机），让他能说话，能触发亚健康状态。

### 第四步：多个角色互动
让两个角色在同一个房间里互相对话。

---

## 开发原则

1. **不要改引擎核心代码**，尽量通过扩展 schema 和加上层逻辑实现
2. **先跑通 MVP，再优化细节
3. **仓库里其他旧文件不用管**，专注 `rpg-mcp-engine/` 就行

---

## 开始

先 cd 到 `rpg-mcp-engine/`，跑起来。
